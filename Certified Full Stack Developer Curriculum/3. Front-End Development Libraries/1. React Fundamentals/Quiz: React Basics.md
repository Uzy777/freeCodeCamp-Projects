---

Questions

Q1) Which of the following is not a JavaScript library or framework?

- Angular
- React
- JQuery
- Laravel

**Answer: Laravel**

---

Q2) Which of the following is NOT a method for conditional rendering in React?

- Ternary operators.
- if statements.
- Logical && operator.
- setTimeout

**Answer: setTimeout**

---

Q3) What is the primary purpose of adding a key prop when rendering a list in React?

- To help React identify items for efficient rendering.
- To apply a unique CSS style to each list item.
- To serve as the text content for the element.
- To add an event listener to each specific item in the list.

**Answer: To help React identify items for efficient rendering.**

---

Q4) Where do the project's packages and libraries get installed?

- The package.json file.
- The src folder.
- The user directory of your computer.
- The node_modules folder.

**Answer: The node_modules folder.**

---

Q5) Which of the following is TRUE about JSX?

- JSX must always be written in separate files from JavaScript.
- JSX cannot use JavaScript expressions.
- JSX is not recommended for React applications.
- JSX allows writing HTML within JavaScript code.

**Answer: JSX allows writing HTML within JavaScript code.**

---

Q6) Which of the following is a common problem with SPAs?

- Browser caching becomes unreliable due to frequent DOM updates.
- Server resources are used more heavily due to constant page rendering.
- Multiple page loads create inconsistent state management.
- Screen readers struggle with dynamically updated content.

**Answer: Screen readers struggle with dynamically updated content.**

---

Q7) How does a single-page application typically update the view?

- By reloading the entire HTML document.
- By using server-side rendering only.
- By updating only the necessary parts of the page.
- By making a full server reload.

**Answer: By updating only the necessary parts of the page.**

---

Q8) Which of the following commands starts a React project in development mode?

- npm run develop
- npm run dev
- npm install
- npm run build

**Answer: npm run dev**

---

Q9) Which of the following is not a valid JSX element?

- <button>Click Me</button>
- <span>Hello World</span>
- <div className='class'></div>
- <img class='img'>

**Answer: <img class='img'>**

---

Q10) Which JavaScript array method is commonly used to transform an array of data into an array of JSX elements?

- forEach()
- map()
- filter()
- reduce()

**Answer: map()**

---

Q11) Which of the following is the best practice for passing the key prop in React?

- Omit the key prop altogether.
- Use unique and stable IDs from the data.
- Randomly generate keys on each render.
- Use array indexes in all cases.

**Answer: Use unique and stable IDs from the data.**

---

Q12) What command installs dependencies listed in package.json?

- npm update
- npm build
- npm run
- npm install

**Answer: npm install**

---

Q13) How would you pass a name prop to a child component?

- <ChildComponent {props} />
- <ChildComponent {'John'} />
- <ChildComponent props="name" />
- <ChildComponent name="Vignesh" />

**Answer: <ChildComponent name="Vignesh" />**

---

Q14) What is the correct way to pass multiple props using the spread operator in React?

- <Child spreader={...person} />.
- <Child {...person} />.
- <Child person={name..., age..., city...} />.
- <Child spread={person} />.

**Answer: <Child {...person} />.**

---

Q15) What are the building blocks of React?

- Classes and Objects.
- DOM Elements and Events.
- Reusable Components.
- Functions and Modules.

**Answer: Reusable Components.**

---

Q16) What is the correct way to add an inline style in React?

- style={{ color: 'blue' }}
- style={{ color: blue }}
- style="color: blue"
- style: { color: 'blue' }

**Answer: style={{ color: 'blue' }}**

---

Q17) What is the correct way to conditionally render a React component?

- Using while loops inside JSX.
- Using if statements inside JSX.
- Declaring a new component inside a render() method.
- Using ternary operators or logical && operators within JSX.

**Answer: Using ternary operators or logical && operators within JSX.**

---

Q18) Which keyword is used to bring a component into another file?

- import
- require
- add
- include

**Answer: import**

---

Q19) What is the full meaning of SPAs?

- Static Progressive Apps.
- Single-page Applications.
- Selective Page Applications.
- Server Page Architectures.

**Answer: Single-page Applications.**

---

Q20) Which extension of JavaScript syntax is used to describe the UI in React?

- JSX
- HTML+
- TSC
- ReactML

**Answer: JSX**

---