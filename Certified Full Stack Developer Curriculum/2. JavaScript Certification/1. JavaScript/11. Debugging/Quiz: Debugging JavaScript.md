---

Questions

Q1) What type of error occurs when a variable is used before it is defined?

- ReferenceError
- RangeError
- SyntaxError
- LogicalError

**Answer: ReferenceError**

---

Q2) Which JavaScript statement can be used to intentionally pause the execution of your code for debugging purposes?

- try...catch
- alert()
- console.log()
- debugger

**Answer: debugger**

---

Q3) Which console method can be used to display data as a table?

- console.warn()
- console.table()
- console.log()
- console.error()

**Answer: console.table()**

---

Q4) How can you inspect network requests in Chrome developer tools?

- By using the "Network" tab.
- By using the "Network Links" tab.
- By using the "Console" tab.
- By using the "Sources" tab.

**Answer: By using the "Network" tab.**

---

Q5) What is the correct syntax for using try...catch in JavaScript?

- try { ... } stack(catch) { ... }
- try { ... } catch(error) { ... }
- try { ... } error { ... }
- try { ... } else { ... }

**Answer: try { ... } catch(error) { ... }**

---

Q6) Which of the following is an example of a SyntaxError?

- Missing a comma between elements of an array.
- Using a variable twice in the same scope.
- Using a variable that is out of scope.
- Using a variable before it is declared.

**Answer: Missing a comma between elements of an array.**

---

Q7) What is the purpose of breakpoints in JavaScript debugging?

- Prevent code from being executed.
- Pause execution at a specific point.
- Automatically correct code mistakes.
- Log messages to the console.

**Answer: Pause execution at a specific point.**

---

Q8) What happens when the throw statement is executed?

- An exception is thrown and the catch block (if present) is executed.
- It pauses code execution until resumed by disabling breakpoints.
- The program terminates immediately, ignoring all remaining code.
- The throw statement logs an error to the console but does not stop execution.

**Answer: An exception is thrown and the catch block (if present) is executed.**

---

Q9) Which method would you use to inspect variables or expressions during debugging in developer tools?

- console.error()
- console.log()
- trace()
- inspect()

**Answer: console.log()**

---

Q10) What type of error will occur when the following code is executed?


function example() {
console.log(a);
let a = 5;
}
example();

- ReferenceError
- FunctionError
- Stack overflow error
- SyntaxError

**Answer: ReferenceError**

---

Q11) Which tool is commonly used to step through code line by line during debugging?

- alert()
- Breakpoints in Developer Tools.
- throw statement.
- console.log()

**Answer: Breakpoints in Developer Tools.**

---

Q12) What type of error occurs when attempting to access a property of undefined?

- LogicalError
- RangeError
- TypeError
- SyntaxError

**Answer: TypeError**

---

Q13) What does the finally block do in a try...catch statement?

- Only executes if there's an error.
- Exits the code block immediately.
- Always executes, regardless of errors.
- Skips execution if catch is triggered.

**Answer: Always executes, regardless of errors.**

---

Q14) What would be the result of running the following code?


const arr = [];
arr.length = -1;

- The code will throw an out of memory error.
- The code will log an empty array to the console.
- The code will throw a SyntaxError.
- The code will throw a RangeError.

**Answer: The code will throw a RangeError.**

---

Q15) Which of the following answers is true about the debugger statement in JavaScript?

- The debugger statement is ignored when the developer tools are not open.
- The debugger statement always reloads the page and clears the console.
- The debugger statement sends an error report to the server.
- The debugger statement is always ignored by the browser.

**Answer: The debugger statement is ignored when the developer tools are not open.**

---

Q16) What is the primary purpose of "profiling" in JavaScript?

- To write unit tests for JavaScript functions and create profiles of code execution.
- To pause code execution and release the captured resources like memory and CPU.
- To identify performance bottlenecks by recording CPU usage, function calls, and execution time.
- To convert JavaScript code into machine code for faster execution.

**Answer: To identify performance bottlenecks by recording CPU usage, function calls, and execution time.**

---

Q17) What is a "watcher" in the context of debugging?

- A JavaScript method for handling exceptions.
- A tool for monitoring the value of variables.
- A tool for pausing code execution.
- A built-in function for logging errors.

**Answer: A tool for monitoring the value of variables.**

---

Q18) When should you use console.error() instead of console.log()?

- To log error messages.
- To log messages only during debugging.
- To log information about the browser.
- To display table data.

**Answer: To log error messages.**

---

Q19) What does the Error() constructor do in JavaScript?

- Runs code in a sandbox.
- Creates a new error object that can be thrown.
- Terminates the program.
- Logs error messages to the console.

**Answer: Creates a new error object that can be thrown.**

---

Q20) What is the purpose of the console.dir() method in JavaScript?

- To output a hierarchical, interactive list of properties for a specified JavaScript object.
- To generate a visual representation of the console output and save it in a pdf file.
- To stop the program execution and display an error message related to memory and CPU usage.
- To create a separate directory for log files for individual error type.

**Answer: To output a hierarchical, interactive list of properties for a specified JavaScript object.**

---