---

Questions

Q1) What is a graph?

- A linear data structure that follows the Last-In, First-Out principle.
- A data structure used to represent the connections or relationships between objects or entities.
- A linear data structure that follows the First-In, First-Out principle.
- A data structure that stores multiple values of the same type that are contiguous in memory.

**Answer: A data structure used to represent the connections or relationships between objects or entities.**

---

Q2) Which of the following is NOT a typical scenario that can be represented using a graph?

- temperature measurement over time
- social networks
- transportation networks
- communications networks

**Answer: temperature measurement over time**

---

Q3) Which of the following terms describes an object or entity that is part of the network depicted by a graph?

- Endpoint
- CPU
- Cookie
- Node

**Answer: Node**

---

Q4) Which of the following terms describes the connections between nodes in a graph?

- Gates
- Edges
- Vertices
- Connectors

**Answer: Edges**

---

Q5) What is an undirected graph?

- A graph where the edges have a specific direction.
- A graph where the nodes are undirected but the edges are directed.
- A graph where the edges don't have a specific direction.
- A graph where the nodes and edges connect at the midpoint.

**Answer: A graph where the edges don't have a specific direction.**

---

Q6) Which of the following refers to a graph in which each node is associated with a label or identifier in addition to its data?

- Version labeled graph
- Virtual labeled graph
- Vertical labeled graph
- Vertex labeled graph

**Answer: Vertex labeled graph**

---

Q7) Which of the following refers to a directed graph with at least one cycle?

- Cyclic graph
- Circular graph
- Cycling graph
- Circulating graph

**Answer: Cyclic graph**

---

Q8) What is a weighted graph?

- A specific type of edge labeled graph that shows only the direction of connections between nodes.
- A specific type of edge labeled graph in which the labels on the edges represent values that can be compared and used to perform arithmetic operations.
- A specific type of edge labeled graph that assigns categories instead of numerical values to edges.
- A specific type of edge labeled graph that treats all edge weights as equal regardless of label.

**Answer: A specific type of edge labeled graph in which the labels on the edges represent values that can be compared and used to perform arithmetic operations.**

---

Q9) What is a directed acyclic graph?

- A directed graph with two cycles.
- A directed graph with no cycles.
- A directed graph with three cycles.
- A directed graph with one cycle.

**Answer: A directed graph with no cycles.**

---

Q10) Which of the following refers to a graph with two or more groups of nodes that are not connected by any edges?

- Disconnect graph
- Disconnecting graph
- Disconnection graph
- Disconnected graph

**Answer: Disconnected graph**

---

Q11) What is the Breadth-first search algorithm?

- An algorithm that visits all nodes in one direction before moving another direction in the graph.
- An algorithm that visits two nodes in one direction before moving another direction in the graph.
- An algorithm that visits four nodes at a time before moving to the next level in the graph.
- An algorithm that visits all neighboring nodes before moving to the next level in the graph.

**Answer: An algorithm that visits all neighboring nodes before moving to the next level in the graph.**

---

Q12) Which of the following algorithms will follow each branch as deep as possible before it backtracks?

- Daemon-First Search
- Depth-First Search
- Data-First Search
- Dock-First Search

**Answer: Depth-First Search**

---

Q13) Which of the following terms represents a two-dimensional list in which the rows and columns represent the graph's vertices?

- Vertex matrix
- Array matrix
- Adjacency matrix
- Depth matrix

**Answer: Adjacency matrix**

---

Q14) Which of the following terms represents an array or dictionary that stores all the neighbors of each node?

- Tuple list
- Cyclic list
- Breadth list
- Adjacency list

**Answer: Adjacency list**

---

Q15) What must be true for a graph to be classified as a tree?

- It must have three loops or cycles.
- It cannot have loops or cycles.
- It must have at least one loop or cycle.
- It must have at least two loops or cycles.

**Answer: It cannot have loops or cycles.**

---

Q16) Which of the following is a type of tree in which each node can have at most two child nodes?

- Linear tree
- Depth tree
- Binary tree
- Spatial tree

**Answer: Binary tree**

---

Q17) What is a Trie?

- A tree data structure used to store a set of strings.
- A tree data structure used to store a set of numbers.
- A tree data structure used to store a set of dictionaries.
- A tree data structure used to store a set of lists.

**Answer: A tree data structure used to store a set of strings.**

---

Q18) How does the priority queue data structure differ from a regular queue data structure?

- A priority queue automatically removes duplicates before insertion.
- A priority queue processes elements based on their priority.
- A priority queue only stores elements of the same data type.
- A priority queue always processes elements in the order they were added.

**Answer: A priority queue processes elements based on their priority.**

---

Q19) What are the two types of heaps?

- Horizontal-heap and Vertical-heap
- Max-heap and Min-heap
- Breadth-heap and Depth-heap
- Wide-heap and Tall-heap

**Answer: Max-heap and Min-heap**

---

Q20) What is the difference between a standard queue and a priority queue?

- In a standard queue, elements are processed based on their priority, while a priority queue processes elements strictly in the order they are added (FIFO).
- Elements are processed based on their priority in a standard queue, while a priority queue processes elements strictly in the order they are added (FIFO).
- In a standard queue, duplicate elements are automatically removed, while a priority queue allows duplicates.
- In a standard queue, elements are processed strictly in the order they are added (FIFO), while a priority queue processes elements based on their priority rather than just insertion order.

**Answer: In a standard queue, elements are processed strictly in the order they are added (FIFO), while a priority queue processes elements based on their priority rather than just insertion order.**

---